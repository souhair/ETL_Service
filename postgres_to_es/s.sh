#!/bin/bash
echo '####################################################'
docker stop $(docker ps -aq)
echo '####################################################'
docker rm $(docker ps -aq)
echo '####################################################'
docker volume rm $(docker volume ls -q)
echo '####################################################'